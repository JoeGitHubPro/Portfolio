@model IEnumerable<Portfolio.EducationTopic>

@{
    ViewBag.Title = "Education";
}

<h2>Education</h2>
<hr />

<p>
    @Html.ActionLink("Create New", "Create", new { }, new { @class = "btn btn-warning" })

</p>
<br />

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EducationCategory.EducationCategory1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EducationTopic1)
        </th>

        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EducationCategory.EducationCategory1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EducationTopic1)
            </td>

            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ID }, new { @class = "btn btn-info" }) |
                @*@Html.ActionLink("Details", "Details", new { id = item.ID }) |*@
                @Html.ActionLink("Delete", "Delete", new { id = item.ID }, new { @class = "btn btn-danger" })
            </td>
        </tr>
    }

</table>
